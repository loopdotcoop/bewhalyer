# Declare variables with application-scope
p   = {} # a read-only object for current `Pram` instances, eg `p.bgStart`
$el = {} # references to page elements
l   = {} # an object for `LoadedTexture` instances
c   = {} # an object for `Color` instances
window.shapes = s   = {} # an object for `Shape` instances

